import { CharactersResponse } from './characters-response';

describe('CharactersResponse', () => {
  it('should create an instance', () => {
    expect(new CharactersResponse()).toBeTruthy();
  });
});
